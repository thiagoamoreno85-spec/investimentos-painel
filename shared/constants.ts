/** Breakpoint mobile em pixels */
export const MOBILE_BREAKPOINT = 768;

/** Taxa USD/BRL padrão quando a API não está disponível */
export const DEFAULT_USD_BRL_RATE = 5.7;

/** Limite padrão de itens por página */
export const PAGINATION_DEFAULT_LIMIT = 20;

/** Intervalo de atualização de notícias em ms (30 minutos) */
export const NEWS_REFRESH_INTERVAL_MS = 30 * 60 * 1000;

/** Máximo de contagem exibida em badges */
export const MAX_BADGE_COUNT = 99;

/** TTL do cache de cotações em ms (5 minutos) */
export const QUOTE_CACHE_TTL_MS = 5 * 60 * 1000;

/** Tamanho máximo do body de requisições */
export const MAX_BODY_SIZE = "50mb";
